import convertDistance from "../utils/convertDistance";

export default function WorkoutTable() {
  return (
    <section>
      <h2>Workout History</h2>
      {/* Table: type, distance, unit, date */}
    </section>
  );
}
